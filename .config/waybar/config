{
/* General config */
  "layer": "top",
  "position": "top",
  "height": 30,
  "margin-top": 0,
  "margin-right": 0,
  "margin-left": 0,
  "style": "/home/ivory/.config/waybar/colors.css",
  "reload_style_on_change": true,

/* Module Placement */
  "modules-left": ["clock", "custom/calendar"],
  "modules-center": ["mpris"],
  "modules-right": ["network", "pulseaudio", "tray", "custom/notification"],

/* App launcher / Taskbar Modules */
 "custom/rofi": {
	"format": "Run",
	"on-click": "wofi --show drun"
  },
    "window": {
        "format": "{}",
        "max-length": 80,
        "tooltip": true,
        "tooltip-format": "{}"
    },
  "niri/workspaces": {
    "on-click": "activate"
  },

  "wlr/taskbar": {
    "format": "{icon} {app_id}",
    "icon-size": 16,
    "on-click": "minimize-raise"
  },

/* Network Waybar Module */
  "network": {
	"interval": 1,
  "format-disconnected": "lf network",
  "tooltip-format-ethernet": "󰈀   Ethernet\nIP: {ipaddr}",
	"format-ethernet": "󰕒  {bandwidthUpBytes}  󰇚  {bandwidthDownBytes}",
	"on-click-right-release": "nm-connection-editor"
  },

/* Media Waybar Modules */
  "pulseaudio": {
    "format": "󰕾  {volume}",
    "on-click": "pavucontrol"
  },

/* Clock Module */
  "clock": {
    "format": "{:%I:%M %p}"
    },

    "custom/calendar": {
        "exec": "date '+%a, %b %d'",
        "interval": 60
    },

/* System Tray Module */
  "tray": {
    "icon-size": 14,
    "spacing": 5,
	"show-passive-items": true
  },

  "cpu": {
      "format": "CPU: {usage}%",
      "interval": 2
  },

  "custom/gpu": {
        "format": "GPU: {}MiB",
        "exec": "nvidia-smi --query-gpu=memory.used --format=csv,noheader,nounits | awk '{print $1}'",
        "interval": 2
      },

  "memory": {
        "format": "RAM: {}%",
        "format-alt": "RAM: {used}/{total}G",
        "interval": 5
      },
"mpris": {
    "format": "󰝚   {title} • {artist}",
    "format-paused": "   {title} • {artist}",
    "on-click": "playerctl play-pause",
    "on-scroll-up": "playerctl next",
    "on-scroll-down": "playerctl previous"
},
  "custom/notification": {
      "format": "  {}",
      "exec": "swaync-client -c",
      "tooltip": true,
      "interval": 1,
      "on-click": "swaync-client -t",
      "signal": 1,
      "format-icons": {
          "default": ""
    }
  }
}
